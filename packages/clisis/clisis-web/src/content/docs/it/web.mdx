---
title: Web
description: Usa Clisis Coder nel browser.
---

Clisis Coder puo funzionare come applicazione web nel browser, offrendo la stessa potente esperienza di coding AI senza bisogno di un terminale.

![Clisis Coder Web - Nuova sessione](../../../assets/web/web-homepage-new-session.png)

## Per iniziare

Avvia l'interfaccia web eseguendo:

```bash
Clisis Coder web
```

Questo avvia un server locale su `127.0.0.1` con una porta disponibile scelta a caso e apre automaticamente Clisis Coder nel browser predefinito.

:::caution
Se `Clisis Coder_SERVER_PASSWORD` non e impostata, il server non sara protetto. Va bene per l'uso locale, ma va impostata per l'accesso in rete.
:::

:::tip[Utenti Windows]
Per la migliore esperienza, esegui `Clisis Coder web` da [WSL](/docs/windows-wsl) invece che da PowerShell. Questo garantisce accesso corretto al file system e integrazione col terminale.
:::

---

## Configurazione

Puoi configurare il server web con i flag della riga di comando o nel tuo [file di config](/docs/config).

### Porta

Per impostazione predefinita, Clisis Coder sceglie una porta disponibile. Puoi specificare una porta:

```bash
Clisis Coder web --port 4096
```

### Nome host

Per impostazione predefinita, il server si lega a `127.0.0.1` (solo localhost). Per rendere Clisis Coder accessibile sulla rete:

```bash
Clisis Coder web --hostname 0.0.0.0
```

Usando `0.0.0.0`, Clisis Coder mostrera sia l'indirizzo locale sia quello di rete:

```
  Local access:       http://localhost:4096
  Network access:     http://192.168.1.100:4096
```

### Scoperta mDNS

Abilita mDNS per rendere il server individuabile sulla rete locale:

```bash
Clisis Coder web --mdns
```

Questo imposta automaticamente l'hostname a `0.0.0.0` e pubblicizza il server come `Clisis Coder.local`.

Puoi personalizzare il nome di dominio mDNS per eseguire piu istanze sulla stessa rete:

```bash
Clisis Coder web --mdns --mdns-domain myproject.local
```

### CORS

Per consentire domini aggiuntivi per CORS (utile per frontend personalizzati):

```bash
Clisis Coder web --cors https://example.com
```

### Autenticazione

Per proteggere l'accesso, imposta una password usando la variabile d'ambiente `Clisis Coder_SERVER_PASSWORD`:

```bash
Clisis Coder_SERVER_PASSWORD=secret Clisis Coder web
```

Lo username predefinito e `Clisis Coder`, ma puo essere cambiato con `Clisis Coder_SERVER_USERNAME`.

---

## Usa l'interfaccia web

Una volta avviata, l'interfaccia web ti da accesso alle tue sessioni Clisis Coder.

### Sessioni

Visualizza e gestisci le sessioni dalla homepage. Puoi vedere le sessioni attive e avviarne di nuove.

![Clisis Coder Web - Sessione attiva](../../../assets/web/web-homepage-active-session.png)

### Stato del server

Fai clic su "See Servers" per vedere i server connessi e il loro stato.

![Clisis Coder Web - See Servers](../../../assets/web/web-homepage-see-servers.png)

---

## Collegare un terminale

Puoi collegare una TUI da terminale a un server web gia in esecuzione:

```bash
# Avvia il web server
Clisis Coder web --port 4096

# In un altro terminale, collega la TUI
Clisis Coder attach http://localhost:4096
```

Questo ti permette di usare contemporaneamente interfaccia web e terminale, condividendo le stesse sessioni e lo stesso stato.

---

## File di configurazione

Puoi anche configurare le impostazioni del server nel file di config `Clisis Coder.json`:

```json
{
  "server": {
    "port": 4096,
    "hostname": "0.0.0.0",
    "mdns": true,
    "cors": ["https://example.com"]
  }
}
```

I flag della riga di comando hanno precedenza sulle impostazioni del file di config.
